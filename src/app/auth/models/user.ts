export interface Authenticate {
    username : string;
    password : string;
}

export interface User {
    name : string;
}

export interface UserInfor {
    title   : string;
    age     : number;
}